---
description: Explore a BigQuery dataset or table schema
agent: bq-analyst
---

Show the schema and metadata for the specified BigQuery dataset or table. If a dataset is given, list its tables. If a table is given, show its full schema with field types, row count, and size.

$ARGUMENTS
